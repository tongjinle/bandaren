<view class="gallery-item">
  <view class="info" wx:if="{{!isShowDetail}}">
    <image class="logo" src="{{logoUrl}}"></image>
    <view class="center">
      <text class="title">{{title}}</text>
      <text class="count">浏览量: {{count}}</text>
    </view>
    <view class="right">
      <image class="more" src="../../assert/images/rightArrow.png" bindtap="more"></image>
    </view>
  </view>
</view>